<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.softmeth.rucafe">

    <application
            android:allowBackup="true"
            android:icon="@drawable/dining"
            android:label="@string/app_name"
            android:roundIcon="@drawable/dining"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".StoreOrderActivity"
                  android:parentActivityName=".MainActivity"/>
        <activity android:name=".OrderActivity"
                  android:parentActivityName=".MainActivity"/>
        <activity
                android:name=".DonutOrderActivity"
                android:parentActivityName=".DonutActivity"/>
        <activity
                android:name=".DonutActivity"
                android:parentActivityName=".MainActivity"/>
        <activity
                android:name=".CoffeeActivity"
                android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
